
def city_country_names(city_name, country_name):
    a = f"{city_name}, {country_name}"
    return a


def city_country_population(city_name, country_name, population):
    a = f"{city_name}, {country_name} {population}"
    return a
